const userInfo = ["username", "name", "email"];

const cart = [50, 10, 0.2];

const profile = [
  { bio: "St. Peters", status: " Available", avatar: "laughing" },
];

delete profile["bio"];
console.log(profile);

